
class Product2 {
  final String image, namepd;
  final String number;


  Product2({
    this.namepd,
    this.image,
    this.number,
  });
}

class Ndata {
  final String id, pic, name, detail, catego, number, price, map, video;

  const Ndata(this.id, this.pic, this.name, this.detail, this.catego, this.number, this.price, this.map, this.video);

}

